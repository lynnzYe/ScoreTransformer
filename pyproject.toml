[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scoretransformer"
version = "0.1.0"
description = "Meter-relaxed ScoreTransformer"
dependencies = []